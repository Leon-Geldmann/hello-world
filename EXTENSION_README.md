# 价格转换器 Chrome 扩展

这是一个Chrome浏览器扩展，可以在网页中的商品价格下方自动添加以黄金和比特币计价的等值价格，每分钟自动更新一次。

## 功能特点

- ✨ 自动检测网页中的价格（支持多种货币格式）
- 💰 实时显示比特币和黄金等值价格
- 🔄 每分钟自动更新价格
- 🌐 支持多种货币：美元($)、人民币(¥)、欧元(€)、英镑(£)
- 🎨 精美的渐变色UI设计
- 📱 响应式设计，支持移动端

## 安装方法

### 1. 准备图标文件

在安装扩展之前，你需要为扩展添加图标。在 `icons/` 文件夹中添加以下三个图标文件：

- `icon16.png` (16x16 像素)
- `icon48.png` (48x48 像素)
- `icon128.png` (128x128 像素)

你可以使用以下工具创建图标：
- [Favicon Generator](https://favicon.io/)
- [Icon Generator](https://www.iconsgenerator.com/)
- 或使用任何图形编辑软件（如Photoshop、GIMP等）

**图标建议：** 使用与货币、比特币或黄金相关的图标，如 💰、₿、🏆 等emoji转换为图标。

### 2. 加载扩展到Chrome

1. 打开 Chrome 浏览器
2. 访问 `chrome://extensions/`
3. 开启右上角的"开发者模式"
4. 点击"加载已解压的扩展程序"
5. 选择本项目的根目录（包含 `manifest.json` 的文件夹）
6. 扩展将被加载并激活

## 使用方法

### 自动转换价格

1. 访问任何包含商品价格的网页（如电商网站）
2. 扩展会自动检测页面中的价格
3. 在原价格下方会显示：
   - ₿ 比特币等值
   - 🏆 黄金克数等值

### 查看当前价格

1. 点击浏览器工具栏中的扩展图标
2. 弹出窗口会显示：
   - 当前比特币价格（美元）
   - 当前黄金价格（每盎司美元）
   - 最后更新时间
3. 可点击"刷新价格"按钮手动更新

## 支持的价格格式

扩展可以识别以下价格格式：

- **美元：** `$123.45`, `$1,234.56`, `USD 123.45`
- **人民币：** `¥123.45`, `￥1,234.56`, `123.45元`, `CNY 123.45`
- **欧元：** `€123.45`, `123.45€`, `EUR 123.45`
- **英镑：** `£123.45`, `GBP 123.45`

## 技术说明

### 价格数据来源

- **比特币价格：** CoinGecko API
- **黄金价格：** CoinGecko API (PAX Gold / Tether Gold)

### 更新频率

- 自动更新：每60秒
- 手动更新：点击弹出窗口中的刷新按钮

### 文件结构

```
.
├── manifest.json       # 扩展配置文件
├── background.js       # 后台脚本（获取价格数据）
├── content.js          # 内容脚本（检测和转换价格）
├── popup.html          # 弹出窗口HTML
├── popup.js            # 弹出窗口脚本
├── styles.css          # 样式文件
└── icons/              # 图标文件夹
    ├── icon16.png
    ├── icon48.png
    └── icon128.png
```

## 权限说明

扩展需要以下权限：

- `storage` - 存储价格数据
- `alarms` - 设置定时更新
- `https://api.coingecko.com/*` - 获取价格数据

## 注意事项

1. **API限制：** 使用的是免费API，有请求频率限制。扩展设置为每分钟更新一次，符合限制要求。

2. **价格准确性：** 显示的价格仅供参考，实际交易请以官方价格为准。

3. **货币转换：** 内置的货币转换率是近似值，实际汇率可能有波动。

4. **隐私：** 扩展不收集任何个人数据，所有价格数据仅保存在本地。

## 开发和调试

### 查看日志

1. 访问 `chrome://extensions/`
2. 找到"价格转换器"扩展
3. 点击"service worker"查看后台日志
4. 在任何网页上右键 > 检查 > Console，查看内容脚本日志

### 修改代码

修改代码后，需要重新加载扩展：
1. 访问 `chrome://extensions/`
2. 找到扩展并点击刷新图标

## 自定义

### 修改货币转换率

编辑 `content.js` 中的 `currencyRates` 对象：

```javascript
const currencyRates = {
  'USD': 1,
  'CNY': 0.14,  // 修改这里
  'EUR': 1.09,
  // ...
};
```

### 修改更新频率

编辑 `background.js` 中的定时器设置：

```javascript
chrome.alarms.create('updatePrices', { periodInMinutes: 1 }); // 修改分钟数
```

### 修改显示样式

编辑 `styles.css` 文件来自定义价格显示的外观。

## 问题排查

### 价格不显示

1. 检查网络连接
2. 查看控制台是否有错误
3. 尝试点击扩展图标手动刷新价格

### 检测不到价格

某些网站使用特殊的价格格式，可能无法自动识别。可以编辑 `content.js` 中的 `pricePatterns` 数组添加新的正则表达式。

## 许可证

本项目仅供学习和个人使用。

## 贡献

欢迎提交Issue和Pull Request！
